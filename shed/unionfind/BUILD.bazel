load("//bazel:rust.bzl", "rust")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

rust.library(
    name = "unionfind",
    srcs = ["src/lib.rs"],
)

rust.test(
    name = "unionfind_test",
    size = "small",
    crate = ":unionfind",
)
